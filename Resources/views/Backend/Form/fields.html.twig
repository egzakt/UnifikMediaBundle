{% block media_select_widget %}
    <input class="input-media" type="hidden" {{ block('widget_attributes') }} {% if media %}value="{{ media.id }}"{% endif %} />
    <a class="select-media" data-media-type="{{ types | json_encode() }}">
        <img class="image-media" src="{% if media %}{{ media.thumbnailUrl | imagine_filter('media_thumb') }}{% endif %}">
        <span class="media_button blue">{% trans %}Select a media{% endtrans %}</span>
    </a>
    <a class="create-media"><span class="media_button green">{% trans %}Upload a new media{% endtrans %}</span></a>

    <script>
        $().ready(function(){
            if('undefined' == typeof flexyMediaScript) {
                var loader = new DynamicLoader();
                loader.addScript('flexymediascript', '/bundles/flexymedia/backend/js/media_select.js');
                loader.addStyle('flexymediastyle', '/bundles/flexymedia/backend/css/media_select.css');
                loader.load(function(){
                    $.mediaManager();
                });
            }
        });
    </script>
{% endblock %}